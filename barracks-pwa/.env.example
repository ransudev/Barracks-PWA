DATABASE_URL=postgres://postgres:postgres@localhost:5432/barracks
DATABASE_SSL=false
DATABASE_POOL_MAX=10

# Keep this value server-only. Do not expose it through NEXT_PUBLIC_*.
ADMIN_API_TOKEN=replace-with-a-long-random-token
