"use client";

import { useCallback, useEffect, useMemo, useState, type FormEvent } from "react";
import type { ApiInventoryItem, ApiSupplier } from "@/app/lib/api";
import { apiRequest, readApiBody } from "@/app/lib/api";
import { Badge, Button, EmptyState, MetricCard, Modal, PageHeader, Panel, SelectField, TextField } from "@/app/components/ui";
import { DetailDrawer, DrawerSection, FilterToolbar, RecordCard, ResponsiveTable, ViewToggle, type OperationalViewMode } from "@/app/components/operations/OperationalPrimitives";

type RestockItem = { id: number; inventoryItemId: number; itemName: string; branch: string; requestedQuantity: number; deliveredQuantity: number | null; unitCost: number | string | null };
type Restock = { id: number; supplier_id: number; supplier_name: string; status: "Pending" | "Accepted" | "Preparing" | "Shipped" | "Delivered" | "Received" | "Cancelled"; branch: string; reference: string | null; notes: string; created_at: string; received_at: string | null; items: RestockItem[] };
type DraftLine = { key: number; inventoryItemId: string; quantity: string; unitCost: string }; type CreateForm = { supplierId: string; branch: string; items: DraftLine[]; reference: string; notes: string }; type ReceiveLine = { id: number; deliveredQuantity: string; unitCost: string };
const emptyCreate: CreateForm = { supplierId: "", branch: "", items: [{ key: 1, inventoryItemId: "", quantity: "1", unitCost: "" }], reference: "", notes: "" };
function responseMessage(body: { message?: string; errors?: Record<string, string[]> } | null, fallback: string) { return body?.errors ? Object.values(body.errors).flat().join(" ") || body?.message || fallback : body?.message || fallback; }
function statusTone(status: Restock["status"]): "success" | "warning" | "danger" | "info" { return status === "Received" ? "success" : status === "Cancelled" ? "danger" : status === "Delivered" ? "info" : "warning"; }

export function RestockManagement({ onToast }: { onToast: (message: string) => void }) {
  const [restocks, setRestocks] = useState<Restock[]>([]); const [suppliers, setSuppliers] = useState<ApiSupplier[]>([]); const [inventory, setInventory] = useState<ApiInventoryItem[]>([]); const [loading, setLoading] = useState(true); const [loadError, setLoadError] = useState(""); const [view, setView] = useState<OperationalViewMode>("cards"); const [search, setSearch] = useState(""); const [statusFilter, setStatusFilter] = useState("all"); const [selected, setSelected] = useState<Restock | null>(null);
  const [createOpen, setCreateOpen] = useState(false); const [createForm, setCreateForm] = useState<CreateForm>(emptyCreate); const [creating, setCreating] = useState(false); const [receiving, setReceiving] = useState<Restock | null>(null); const [receiveLines, setReceiveLines] = useState<ReceiveLine[]>([]); const [receiveReference, setReceiveReference] = useState(""); const [receiveNotes, setReceiveNotes] = useState(""); const [receiveSubmitting, setReceiveSubmitting] = useState(false); const [markingDelivered, setMarkingDelivered] = useState<number | null>(null);
  const load = useCallback(async () => { setLoading(true); try { const [restockResponse, supplierResponse, inventoryResponse] = await Promise.all([apiRequest("/api/restocks", { cache: "no-store" }), apiRequest("/api/suppliers", { cache: "no-store" }), apiRequest("/api/inventory", { cache: "no-store" })]); const restockBody = await readApiBody<{ success: boolean; restocks?: Restock[]; message?: string }>(restockResponse); const supplierBody = await readApiBody<{ success: boolean; suppliers?: ApiSupplier[]; message?: string }>(supplierResponse); const inventoryBody = await readApiBody<{ success: boolean; items?: ApiInventoryItem[]; message?: string }>(inventoryResponse); if (!restockResponse.ok || !restockBody?.success) throw new Error(restockBody?.message ?? "Unable to load restock requests"); if (!supplierResponse.ok || !supplierBody?.success) throw new Error(supplierBody?.message ?? "Unable to load suppliers"); if (!inventoryResponse.ok || !inventoryBody?.success) throw new Error(inventoryBody?.message ?? "Unable to load inventory"); setRestocks(restockBody.restocks ?? []); setSuppliers(supplierBody.suppliers ?? []); setInventory(inventoryBody.items ?? []); setLoadError(""); } catch (error) { const message = error instanceof Error ? error.message : "Unable to load restock requests"; setLoadError(message); onToast(message); } finally { setLoading(false); } }, [onToast]);
  useEffect(() => { const frame = window.requestAnimationFrame(() => { void load(); }); return () => window.cancelAnimationFrame(frame); }, [load]);
  const visible = useMemo(() => { const query = search.trim().toLowerCase(); return restocks.filter((restock) => (!query || `${restock.id} ${restock.supplier_name} ${restock.branch} ${restock.reference ?? ""} ${restock.items.map((item) => item.itemName).join(" ")}`.toLowerCase().includes(query)) && (statusFilter === "all" || restock.status === statusFilter)); }, [restocks, search, statusFilter]);
  const pending = restocks.filter((restock) => !["Received", "Cancelled"].includes(restock.status)); const delivered = restocks.filter((restock) => restock.status === "Delivered"); const received = restocks.filter((restock) => restock.status === "Received");
  const supplierBranches = useMemo(() => [...new Set(inventory.filter((item) => item.status === "active" && item.supplierId === Number(createForm.supplierId)).map((item) => item.branch))].sort(), [createForm.supplierId, inventory]);
  function itemsForLine(lineKey: number) { const selectedElsewhere = new Set(createForm.items.filter((line) => line.key !== lineKey).map((line) => Number(line.inventoryItemId))); return inventory.filter((item) => item.status === "active" && item.supplierId === Number(createForm.supplierId) && item.branch === createForm.branch && !selectedElsewhere.has(item.id)); }
  function openCreate() { setCreateForm({ ...emptyCreate, items: [{ ...emptyCreate.items[0], key: Date.now() }] }); setCreateOpen(true); }
  function updateLine(key: number, patch: Partial<DraftLine>) { setCreateForm((current) => ({ ...current, items: current.items.map((line) => line.key === key ? { ...line, ...patch } : line) })); }
  function chooseItem(key: number, value: string) { const item = inventory.find((candidate) => candidate.id === Number(value)); updateLine(key, { inventoryItemId: value, unitCost: item ? String(item.unitCost) : "" }); }
  function openReceive(restock: Restock) { setReceiving(restock); setReceiveLines(restock.items.map((item) => ({ id: item.id, deliveredQuantity: String(item.requestedQuantity), unitCost: item.unitCost === null || item.unitCost === undefined ? "" : String(item.unitCost) }))); setReceiveReference(restock.reference ?? ""); setReceiveNotes(""); }
  async function createRestock(event: FormEvent) { event.preventDefault(); setCreating(true); try { const supplierId = Number(createForm.supplierId); if (!supplierId || !createForm.branch) throw new Error("Choose a supplier and branch"); const items = createForm.items.map((line) => { const quantity = Number(line.quantity); const unitCost = line.unitCost.trim() ? Number(line.unitCost) : null; if (!Number.isInteger(Number(line.inventoryItemId)) || Number(line.inventoryItemId) <= 0) throw new Error("Choose an inventory item for every line"); if (!Number.isInteger(quantity) || quantity <= 0) throw new Error("Requested quantities must be positive whole numbers"); if (unitCost !== null && (!Number.isFinite(unitCost) || unitCost < 0)) throw new Error("Unit costs must be non-negative amounts"); return { inventoryItemId: Number(line.inventoryItemId), requestedQuantity: quantity, unitCost }; }); if (new Set(items.map((item) => item.inventoryItemId)).size !== items.length) throw new Error("An item can only appear once per request"); const response = await apiRequest("/api/restocks", { method: "POST", body: JSON.stringify({ supplierId, branch: createForm.branch, reference: createForm.reference.trim() || null, notes: createForm.notes.trim(), items }) }); const body = await readApiBody<{ success: boolean; message?: string; errors?: Record<string, string[]> }>(response); if (!response.ok || !body?.success) throw new Error(responseMessage(body, "Unable to create restock request")); setCreateOpen(false); onToast("Restock request sent to supplier"); await load(); } catch (error) { onToast(error instanceof Error ? error.message : "Unable to create restock request"); } finally { setCreating(false); } }
  async function markDelivered(restock: Restock) { setMarkingDelivered(restock.id); try { const response = await apiRequest(`/api/restocks/${restock.id}/delivered`, { method: "POST" }); const body = await readApiBody<{ success: boolean; message?: string }>(response); if (!response.ok || !body?.success) throw new Error(body?.message ?? "Unable to confirm delivery"); onToast(`Restock #${restock.id} marked Delivered`); await load(); } catch (error) { onToast(error instanceof Error ? error.message : "Unable to confirm delivery"); } finally { setMarkingDelivered(null); } }
  async function receive(event: FormEvent) { event.preventDefault(); if (!receiving) return; setReceiveSubmitting(true); try { const items = receiveLines.map((line) => { const deliveredQuantity = Number(line.deliveredQuantity); const unitCost = line.unitCost.trim() ? Number(line.unitCost) : null; if (!Number.isInteger(deliveredQuantity) || deliveredQuantity < 0) throw new Error("Delivered quantities must be whole numbers"); if (unitCost !== null && (!Number.isFinite(unitCost) || unitCost < 0)) throw new Error("Unit costs must be non-negative amounts"); return { restockRequestItemId: line.id, deliveredQuantity, unitCost }; }); const response = await apiRequest(`/api/restocks/${receiving.id}/receive`, { method: "POST", body: JSON.stringify({ reference: receiveReference.trim() || null, notes: receiveNotes.trim(), items }) }); const body = await readApiBody<{ success: boolean; message?: string }>(response); if (!response.ok || !body?.success) throw new Error(body?.message ?? "Unable to receive delivery"); setReceiving(null); setSelected(null); onToast("Delivery received and inventory updated"); await load(); } catch (error) { onToast(error instanceof Error ? error.message : "Unable to receive delivery"); } finally { setReceiveSubmitting(false); } }
  function createFormView() { return <form className="modal-form" onSubmit={createRestock}><SelectField required label="Supplier" value={createForm.supplierId} onChange={(event) => setCreateForm({ ...createForm, supplierId: event.target.value, branch: "", items: [{ key: Date.now(), inventoryItemId: "", quantity: "1", unitCost: "" }] })}><option value="">Choose supplier</option>{suppliers.filter((supplier) => supplier.status === "active").map((supplier) => <option key={supplier.id} value={supplier.id}>{supplier.companyName}</option>)}</SelectField><SelectField required label="Branch" value={createForm.branch} onChange={(event) => setCreateForm({ ...createForm, branch: event.target.value, items: createForm.items.map((line) => ({ ...line, inventoryItemId: "", unitCost: "" })) })}><option value="">Choose branch</option>{supplierBranches.map((branch) => <option key={branch} value={branch}>{branch}</option>)}</SelectField><div className="modal-form__section"><div className="section-heading"><div><h3>Request items</h3><p className="form-hint">Items are limited to this supplier and branch.</p></div><Button type="button" variant="secondary" size="sm" icon="plus" onClick={() => setCreateForm((current) => ({ ...current, items: [...current.items, { key: Date.now() + current.items.length, inventoryItemId: "", quantity: "1", unitCost: "" }] }))}>Add item</Button></div>{createForm.items.map((line, index) => <div className="form-grid form-grid--three" key={line.key}><SelectField required label={`Item ${index + 1}`} value={line.inventoryItemId} onChange={(event) => chooseItem(line.key, event.target.value)}><option value="">Choose item</option>{itemsForLine(line.key).map((item) => <option key={item.id} value={item.id}>{item.name} · {item.quantity} {item.unit}</option>)}</SelectField><TextField required label="Quantity" type="number" min="1" step="1" value={line.quantity} onChange={(event) => updateLine(line.key, { quantity: event.target.value })} /><TextField label="Expected unit cost" type="number" min="0" step="0.01" value={line.unitCost} onChange={(event) => updateLine(line.key, { unitCost: event.target.value })} /></div>)}</div><TextField label="Reference" value={createForm.reference} onChange={(event) => setCreateForm({ ...createForm, reference: event.target.value })} /><TextField label="Notes" value={createForm.notes} onChange={(event) => setCreateForm({ ...createForm, notes: event.target.value })} /><div className="modal-actions"><Button type="button" variant="secondary" disabled={creating} onClick={() => setCreateOpen(false)}>Cancel</Button><Button type="submit" disabled={creating}>{creating ? "Sending…" : "Send request"}</Button></div></form>; }

  return <div className="operational-workspace"><PageHeader title="Restock requests" description="Follow every request from supplier handoff through receiving." action={<><ViewToggle view={view} onChange={setView} label="Choose restock view" /><Button icon="plus" onClick={openCreate}>New request</Button></>} /><div className="metrics-grid metrics-grid--four"><MetricCard label="Open requests" value={String(pending.length)} icon="info" accent="amber" /><MetricCard label="Ready to receive" value={String(delivered.length)} icon="box" accent="blue" /><MetricCard label="Received" value={String(received.length)} icon="check" accent="green" /><MetricCard label="Suppliers" value={String(suppliers.filter((supplier) => supplier.status === "active").length)} icon="users" accent="violet" /></div><Panel className="operational-panel"><div className="inventory-catalog-head"><div><span className="inventory-kicker">Supply workflow</span><h2>Restock requests</h2><p>Open a request to see lines, references, and the next safe action.</p></div><div className="inventory-catalog-count"><strong>{visible.length}</strong><span>visible</span></div></div><FilterToolbar search={search} onSearchChange={setSearch} placeholder="Search requests" filters={<SelectField value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)} aria-label="Filter restocks by status"><option value="all">All statuses</option>{["Pending", "Accepted", "Preparing", "Shipped", "Delivered", "Received", "Cancelled"].map((status) => <option key={status} value={status}>{status}</option>)}</SelectField>} resultCount={visible.length} />{loading ? <div className="operational-loading" role="status">Loading restock requests…</div> : loadError ? <div className="operational-loading" role="alert">{loadError}</div> : !visible.length ? <EmptyState icon="box" title="No restock requests" description="Create a request from supplier-linked inventory items." action={<Button size="sm" onClick={openCreate}>New request</Button>} /> : view === "cards" ? <div className="operational-card-grid">{visible.map((restock) => <RecordCard key={restock.id} onOpen={() => setSelected(restock)} ariaLabel={`Open restock request ${restock.id}`}><div className="operational-card__header"><div><span className="inventory-kicker">Request #{restock.id}</span><h3 className="operational-card__title">{restock.supplier_name}</h3><p className="operational-card__caption">{restock.branch} · {restock.reference || new Date(restock.created_at).toLocaleDateString()}</p></div><Badge tone={statusTone(restock.status)}>{restock.status}</Badge></div><p className="operational-card__note">{restock.items.map((item) => `${item.itemName} × ${item.requestedQuantity}`).join(", ")}</p><div className="operational-card__facts"><div><span>Lines</span><strong>{restock.items.length}</strong></div><div><span>Next action</span><strong>{restock.status === "Shipped" ? "Confirm delivery" : restock.status === "Delivered" ? "Receive" : restock.status}</strong></div></div></RecordCard>)}</div> : <ResponsiveTable headers={["Request", "Supplier", "Branch", "Items", "Status", "Next action"]}>{visible.map((restock) => <tr key={restock.id} tabIndex={0} onClick={() => setSelected(restock)} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); setSelected(restock); } }}><td><strong>#{restock.id}</strong><small>{restock.reference || new Date(restock.created_at).toLocaleDateString()}</small></td><td>{restock.supplier_name}</td><td>{restock.branch}</td><td>{restock.items.map((item) => `${item.itemName} × ${item.requestedQuantity}`).join(", ")}</td><td><Badge tone={statusTone(restock.status)}>{restock.status}</Badge></td><td>{restock.status === "Shipped" ? "Confirm delivery" : restock.status === "Delivered" ? "Receive" : "—"}</td></tr>)}</ResponsiveTable>}</Panel><Modal open={createOpen} title="Create restock request" onClose={() => !creating && setCreateOpen(false)}>{createFormView()}</Modal><DetailDrawer open={Boolean(selected)} title={selected ? `Restock request #${selected.id}` : "Restock request"} subtitle={selected ? `${selected.supplier_name} · ${selected.branch}` : undefined} eyebrow="Supply workflow" onClose={() => setSelected(null)}>{selected && <><DrawerSection><div className="operational-drawer__identity"><span className="supplier-card-mark supplier-card-mark--large"><span>#{selected.id}</span></span><div><strong>{selected.supplier_name}</strong><span>{selected.reference || "No reference"}</span></div><Badge tone={statusTone(selected.status)}>{selected.status}</Badge></div></DrawerSection><DrawerSection eyebrow="Request lines" title="Inventory requested"><div className="drawer-mini-list">{selected.items.map((item) => <div key={item.id}><strong>{item.itemName}</strong><span>{item.requestedQuantity} requested · {item.deliveredQuantity ?? 0} delivered</span></div>)}</div></DrawerSection><DrawerSection eyebrow="Actions" title="Move this request forward"><div className="operational-drawer__actions">{selected.status === "Shipped" && <Button variant="secondary" disabled={markingDelivered === selected.id} onClick={() => void markDelivered(selected)}>{markingDelivered === selected.id ? "Confirming…" : "Mark delivered"}</Button>}{selected.status === "Delivered" && <Button icon="box" onClick={() => openReceive(selected)}>Receive delivery</Button>}{!(["Shipped", "Delivered"].includes(selected.status)) && <p className="form-hint">The supplier controls the next status transition. Staff action becomes available once the request is shipped.</p>}</div></DrawerSection></>}</DetailDrawer><Modal open={Boolean(receiving)} title={receiving ? `Receive request #${receiving.id}` : "Receive delivery"} onClose={() => !receiveSubmitting && setReceiving(null)}><form className="modal-form" onSubmit={receive}>{receiving?.items.map((item) => { const line = receiveLines.find((entry) => entry.id === item.id); return <div className="form-grid form-grid--two" key={item.id}><TextField label={`${item.itemName} delivered`} type="number" min="0" step="1" value={line?.deliveredQuantity ?? "0"} onChange={(event) => setReceiveLines((current) => current.map((entry) => entry.id === item.id ? { ...entry, deliveredQuantity: event.target.value } : entry))} /><TextField label="Unit cost" type="number" min="0" step="0.01" value={line?.unitCost ?? ""} onChange={(event) => setReceiveLines((current) => current.map((entry) => entry.id === item.id ? { ...entry, unitCost: event.target.value } : entry))} /></div>; })}<TextField label="Reference number" value={receiveReference} onChange={(event) => setReceiveReference(event.target.value)} /><TextField label="Notes" value={receiveNotes} onChange={(event) => setReceiveNotes(event.target.value)} /><p className="form-hint">Receiving updates every line transactionally and cannot be repeated.</p><div className="modal-actions"><Button type="button" variant="secondary" disabled={receiveSubmitting} onClick={() => setReceiving(null)}>Cancel</Button><Button type="submit" disabled={receiveSubmitting}>{receiveSubmitting ? "Receiving…" : "Confirm receiving"}</Button></div></form></Modal></div>;
}
